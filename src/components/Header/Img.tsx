import styled from 'styled-components';

import NormalImg from '../../components/Img';

const Img = styled(NormalImg)`
  display: block;
  margin: 0;
  height: 38px;
  width: 38px;
`;

export default Img;
